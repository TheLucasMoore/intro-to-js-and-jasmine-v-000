'use strict';

function favIceCream(ice) {
  return "I love " + ice;
}

function shouting(message) {
  return message.toUpperCase();
};

function roundDown(number) {
  return Math.floor(number);
}

function theTruth() {
  return true;
}